# WebSocket URL of the room server, baked into the client at build time.
# Leave unset for a static-only build (hot-seat mode only) or for local dev,
# where the client talks to ws://localhost:9902/ws.
VITE_WS_URL=wss://chacarero.example.com/chacarero/ws
